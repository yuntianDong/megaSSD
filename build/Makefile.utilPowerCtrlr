APP_NAME = utilPowerCtrlr

SRC	= ../src/utils/utilPowerCtrlr/utilPowerCtrlr.py

all: GOAL=all
all: $(APP_NAME)

clean: GOAL=clean
clean: 
	rm -rf dist build
	rm -rf *.spec
	
$(APP_NAME):$(SRC)
	pyinstaller -p ../bin -F $(SRC) && cp dist/$(APP_NAME) ../bin/